# Xcode
# NOTE: VoiceFlow.xcodeproj is committed so the project opens with a double-click.
# Only per-user state and build output are ignored.
*.xcworkspace
xcuserdata/
**/xcuserdata/
DerivedData/
build/

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Packages/

# Secrets — never commit API keys
Secrets.plist
*.env
